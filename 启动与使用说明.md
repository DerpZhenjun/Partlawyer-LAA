# LabourLawyer 三端启动与使用说明

LabourLawyer 面向普通劳动者。使用时先选择省份，再生成当地格式的劳动仲裁申请书或证据清单，并按页面中的官方指南提交材料。

## 一、网页端（推荐先用这个）

### 一键启动

双击仓库根目录的 `start-web.bat`。脚本会自动完成以下工作：

- 首次运行时安装前端和后端依赖；
- 启动文书与 AI 后端：`http://127.0.0.1:8000`；
- 启动网页：`http://127.0.0.1:5173`；
- 启动法律数字人：`http://127.0.0.1:3000`；
- 自动打开浏览器。

关闭全部服务时，双击 `stop-web.bat`。运行日志保存在仓库根目录的 `.runtime/`。

### 基本用法

1. 在首页选择实际工作地或用人单位所在地。
2. 浙江、广东可直接进入申请书或证据清单页面。
3. 填写后点击“生成并下载”；页面会先显示确认框，确认后生成 Word 文件。
4. 右下角“AI 法律顾问”可回答劳动争议问题；聊天框中的“数字人”按钮可打开法律数字人。
5. 页面中的官方办理入口用于查看最新材料要求并进行网上申请。

### AI 模型

AI 默认连接本机 Ollama。启动脚本会优先使用电脑上已经安装的 Qwen 模型；如果尚未安装，推荐先使用兼顾速度和效果的 `qwen3.5:4b`：

```powershell
ollama pull qwen3.5:4b
ollama serve
```

如需更换模型，可在启动前设置 `LABOURLAWYER_LLM_MODEL`；Ollama 地址可通过 `OLLAMA_API_BASE` 设置。

## 二、Android 手机端

### USB 一键安装

1. 手机开启开发者选项和 USB 调试，连接电脑并在手机上允许本机调试。
2. 先双击 `start-web.bat`，保持网页、后端和数字人服务运行。
3. 双击 `install-mobile.bat`。

安装脚本会自动构建网页资源、生成 APK、转发本机服务端口、安装应用并在手机上打开。调试 APK 位于：

`mobile/android/app/build/outputs/apk/debug/app-debug.apk`

如果提示设备为 `offline`，请解锁手机、重新插拔数据线并重新确认“允许 USB 调试”，直到 `adb devices` 显示 `device`，然后再次运行安装脚本。

USB 调试版依赖电脑上的服务。正式上架前，应将 API 和数字人地址替换为正式 HTTPS 域名，并使用正式证书签名 APK/AAB。

## 三、微信小程序

1. 安装并打开微信开发者工具。
2. 选择“导入项目”，目录选择仓库中的 `miniprogram/`。
3. 本地预览可使用测试号；发布前请在 `miniprogram/project.config.json` 配置正式 AppID。
4. 将 `miniprogram/app.js` 中的 `apiBase` 改为正式 HTTPS 后端地址。
5. 在微信公众平台配置该地址为 `request` 合法域名，再进行真机预览、上传和审核。

微信不允许正式小程序直接请求电脑的 `127.0.0.1`。本地开发可以在开发者工具中临时关闭域名校验；提交审核前必须恢复校验并使用备案 HTTPS 域名。

## 常见问题

### 页面提示“无法连接文书生成服务”

先访问 `http://127.0.0.1:8000/health`。如果打不开，重新运行 `start-web.bat`，再查看 `.runtime/backend-error.log`。

### AI 法律顾问无法回答

确认 Ollama 正在运行且已经下载模型。文书生成功能和 AI 功能相互独立，AI 暂时不可用时仍可正常生成 Word 文书。

### 数字人打不开

确认 `http://127.0.0.1:3000` 可以访问。首次启动需要安装数字人依赖，耗时会比平时更长。

### 正式部署

生产环境需配置 HTTPS、身份认证、数据加密、隐私政策、日志审计与备份。身份证号、电话、住址等敏感信息不应写入公开日志，也不应发送给未经授权的第三方服务。
