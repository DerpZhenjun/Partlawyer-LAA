# LabourLawyer 微信小程序

使用微信开发者工具直接导入本目录。首次联调前：

1. 在 `project.config.json` 替换正式小程序 AppID。
2. 在 `app.js` 将 `apiBase` 改为已备案的 HTTPS API 域名。
3. 在微信公众平台配置 request 合法域名，并重新启用域名校验。

小程序面向劳动者提供省份入口、办理指南和 AI 初步梳理。浙江、广东的完整 Word 文书生成当前优先使用网页端或 Android 手机端。
